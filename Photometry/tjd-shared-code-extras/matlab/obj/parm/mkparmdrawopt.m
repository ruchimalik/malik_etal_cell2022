function dopt = mkparmdrawopt(varargin)
% MKPARMDRAWOPT options for drawing e.pos params (behavior)

  dopt = struct(...
      'draw', true,...
      'legend', true,...
      'dispname', [],...
      'bgcolor', [0.7 0.7 0.7],...
      'ptcolor', [0 0 0.8;0.8 0 0],...
      'datalim', [],...
      'subsampnpix', 10,... % # points/pixel to draw [] = plot all
      'marker', 'o',...
      'markersize', 2);
  
  dopt = parseArgsLite(varargin,dopt);